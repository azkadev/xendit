name: xendit_server
description: A simple command-line application.
version: 1.0.0
publish_to: "none"

environment:
  sdk: '>=2.16.0 <3.0.0'

dev_dependencies:
  lints: ^2.0.0
dependencies: 
  hexaminate: ^0.1.0
  qr: ^3.0.0
  xendit:
    path: ../../
